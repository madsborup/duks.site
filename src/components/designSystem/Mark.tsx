import styled from 'styled-components'
import tokens from './tokens'

export default styled.mark`
  color: ${tokens.colors.text};
  background-color: ${tokens.colors.primaryMuted};
  padding: 0 ${tokens.spacing.xxsmall}px;
`